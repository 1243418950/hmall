spring:
  application:
    name: hm-gateway
  cloud:
    nacos:
      server-addr: 39.98.247.178
      config:
        file-extension: yaml
        shared-configs:
          - dataId: shared-log.yaml # 共享日志配置